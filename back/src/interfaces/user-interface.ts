export interface IUser {
  id: number;
  name: string;
  email: string;
  nDni: number;
  birthdate: string;
  // appointments: IAppointment[];
  // credential: ICredential;
  // nDni: number;
  // credentials: string;
  // createdAt: Date;
  // updatedAt: Date;
}
